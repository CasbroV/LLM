GPT-2 Tokenization Analyzer
This project demonstrates how GPT-2 tokenization works. The application takes user input, converts it into tokens using the GPT-2 tokenizer, generates token IDs that can be consumed by an LLM, and then decodes the IDs back to text. It helps understand the preprocessing step used in Large Language Models before inference.
